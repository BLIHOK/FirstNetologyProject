<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FirstNetologyProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FirstNetologyProject" parent="Base.Theme.FirstNetologyProject" />

    <style name="Widget.AppTheme.LikeCheckBox" parent="Widget.Material3.Button.IconButton">

        <item name="maxImageSize">@dimen/icon_size</item>
        <item name="android:iconTint">@color/like_checkbox_tint</item>
        <item name="iconSize">@dimen/icon_size</item>
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">start</item>
        <item name="android:padding">0dp</item>
        app:iconSize="@dimen/icon_size"
        app:iconGravity="start"
        app:iconPadding="0dp"
        android:padding="0dp"

    </style>

</resources>